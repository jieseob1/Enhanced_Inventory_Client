import React from 'react';
import { Page, Card } from '@shopify/polaris';

const Settings = () => {

  return (
    <Page title="Settings">
      <Card>
        <div>settings</div>
      </Card>
    </Page>
  );
};

export default Settings;